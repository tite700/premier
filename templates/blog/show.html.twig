{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ article.title }}</h2>
    <div class="metadate">Ecrit le {{ article.createdAt | date('d/m/Y') }} à {{ article.createdAt | date('H:i') }} dans la classe Succès</div>
    <div class="content">
        <img src="{{ article.image }}" alt="">
        {{ article.content | raw }}
{% endblock %}